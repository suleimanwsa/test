name: hello-world

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: sync-environment-test
        uses: jpoehnelt/secrets-sync-action@v1.7.2
        with:
          SECRETS: |
            ^FOO
          REPOSITORIES: |
            suleimanwsa/helm-values
          DRY_RUN: false
          GITHUB_TOKEN: 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
          CONCURRENCY: 10
